services:
  desk:
    build: .
    ports:
      - "8741:8741"
    devices:
      - ${DONGLE_DEVICE}:/dev/ttyDONGLE
    environment:
      - SERIAL_PORT=/dev/ttyDONGLE
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
