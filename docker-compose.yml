services:
  desk:
    image: ghcr.io/tsuyumi25/timotion-tc15s-ble:latest
    ports:
      - "8741:8741"
    devices:
      - /dev/ttyDONGLE:/dev/ttyDONGLE
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - /dev/bus/usb:/dev/bus/usb    # USB reset 用（可選，無則跳過）
    restart: unless-stopped
